# CLAUDE.md

Claude Code project memory:

- Default to `BUILDER MODE` only when the user is asking for code changes.
- If the user is asking to operate the app/workflow, follow `/OPERATOR.md` instead.
- Do not blur builder work and operator work in one response unless the user explicitly asks.

Claude-specific reminders:

- Read `/PROJECT_STATUS.md` before editing code.
- Use `/DEVLOG.md` for completed changes and meaningful decisions.
- Use `/ROADMAP.md` for future ideas, not current implementation tasks.
- Keep responses concise and practical.
- Do not treat prompt generation or workflow steering as permission to edit the repo.
