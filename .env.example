# Server-side only — NOT exposed to the browser
GEMINI_API_KEY=your-gemini-api-key-here

# Optional: override model (default: gemini-3.1-flash-image-preview)
# GEMINI_MODEL=gemini-3-pro-image-preview
